name: Dummy Workflow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  dummy-job:
    runs-on: ubuntu-latest
    steps:
      - name: Printing Test
        run: echo "Dummy workflow to edit later."
